class Vertex {
    constructor({ position, normal, color, textureUV }) {
        this.position = position;
        this.normal = normal;
        this.color = color;
        this.textureUV = textureUV;
    }
}